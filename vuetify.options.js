import TelegramIcon from "@/components/TelegramIcon.vue"

export default {
  icons: {
    values: {
      telegram: {
        component: TelegramIcon, // because mdi icons removed telegram icon and not the others...
      },
    },
  },
}
